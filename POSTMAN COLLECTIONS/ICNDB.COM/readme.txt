The set of queries to be performed

1. Check if user can receive a full list of Chuck Norris jokes 
2. Check if user can count the quantity of available jokes
3. Check if user can receive preferred Joke by valid id 
4. Check if user can change firstName and lastName within a specific joke
5. Check if user can change firstName and lastName within a random joke
6. Check if user can receive the list of available categories of jokes
7. Check if user can receive jokes with a specific valid category 
8. Check if user can exclude jokes with specific category when receiving jokes
9. Check if user can receive any random joke 
10. Check if user can receive any specific quantity of any random jokes 
